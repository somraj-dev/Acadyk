server:
  port: 8080

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    clean-disabled: false

logging:
  level:
    com.acadyk: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
